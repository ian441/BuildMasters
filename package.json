{
  "name": "contruction-site",
  "version": "1.0.0",
  "description": "Construction site website",
  "main": "index.html",
  "scripts": {
    "start": "live-server",
    "dev": "nodemon --exec live-server",
    "lint": "eslint \"assets/js/*.js\"",
    "lint:fix": "eslint \"assets/js/*.js\" --fix",
    "format": "prettier --write \"**/*.{html,css,js}\"",
    "test": "echo \"No tests yet\""
  },
  "devDependencies": {
    "eslint": "^9.11.1",
    "eslint-config-prettier": "^9.1.0",
    "live-server": "^1.2.2",
    "nodemon": "^3.1.9",
    "prettier": "^3.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
